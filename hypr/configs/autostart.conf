# XCursor & Hyprcursor
# exec-once = gsettings set org.gnome.desktop.interface cursor-theme mocha-cursors
# exec-once = gsettings set org.gnome.desktop.interface icon-theme mocha-icons
# exec-once = gsettings set org.gnome.desktop.interface gtk-theme mocha-gtk
# exec-once = gsettings set org.gnome.desktop.wm.preferences theme mocha-gtk
# exec-once = hyprctl setcursor mocha-cursors 24

# All exec-once files goes here
exec-once = hypridle
exec-once = hyprpaper

# Notification Daemon
exec-once = swaync

# Bluetooth & NetworkManager 
exec-once = nm-applet --indicator
exec-once = blueman-applet

# Authentication Agent
# exec-once = /usr/lib/polkit-kde-authentification-agent-1
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# Status Bar
exec-once = waybar

# Cliphist
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

# Screen Sharing Related Environments
exec-once = ~/.config/hypr/scripts/xdph.sh
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
